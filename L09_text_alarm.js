let csecond, cminute, chour;
let ctime;
function setup(){
    createCanvas(600,400);
    textSize(90);
}

function draw(){
    background('pink');
    csecond = second();
    cminute = minute 
    text(csecond, width/ 2, height/ 2);
}